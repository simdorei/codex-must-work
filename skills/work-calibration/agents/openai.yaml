interface:
  display_name: "Bottleneck threshold guidance"
  short_description: "Route older calibration questions to current work settings"
  default_prompt: "Use $work-settings to show or change Codex Must Work thresholds."
policy:
  allow_implicit_invocation: true

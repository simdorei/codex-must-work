interface:
  display_name: "Work Off"
  short_description: "Stop or complete one managed CMW task"
  default_prompt: "Use $work-off to manually stop or verified-complete only this managed Codex task."
policy:
  allow_implicit_invocation: true
